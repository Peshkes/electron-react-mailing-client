import React from 'react';

type Props = {
    id: number
    type: "message" | "sample"
}

const MessageForm = ({id, type}: Props) => {
    return (
        <div>

        </div>
    );
};

export default MessageForm;